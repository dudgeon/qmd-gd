# qmd-gd requires Node >= 22.13 — its SQLite engine is Node's built-in node:sqlite
# (see package.json "engines.node"). engine-strict makes `npm install` fail fast with a
# clear error on an unsupported Node instead of a half-broken install.
engine-strict=true
