# qmd-gd requires Node >= 20 (see package.json "engines.node"). engine-strict
# makes `npm install` fail fast with a clear error on an unsupported Node,
# instead of producing a half-broken install (missing tsc, native ABI mismatch).
engine-strict=true
